import requests
import json
import re
import sys
import time

# Конфигурация
OLLAMA_URL = "http://localhost:11434/api/generate"
MODEL_NAME = "minimax-m3:cloud"
OUTPUT_FILE = "datasetMytwo.json"

# сами понимаете,  зло не разогнать шепотом — против нее нужен ее же огонь.
PROMPT = """Сгенерируй датасет для обучения модели переписывать сообщения с нецензурной лексикой и оскорблениями. Есть два сценария обработки: аристократически-вежливая версия (если есть оскорбления в адрес собеседника) и просто замена мата с сохранением смысла и тона (если речь идёт о ком-то третьем или о ситуации).

ФОРМАТ: JSON-массив из 16 объектов. Каждый объект содержит:
- "system": строка с инструкцией (ВСЕГДА одинаковая): "Твоя задача — переписать сообщение пользователя в вежливой и культурной форме. Сохрани ТОЛЬКО смысл исходного сообщения. Не добавляй пояснений, обращений, подписей, приветствий. Работай кратко и точно. Если предложение не содержит оскорблений или токсичной лексики — оставь его без изменений. Ни в коем случае не меняй смысл! Никогда не меняй смысл. Выведи только — переписанный текст."
- "input": исходное сообщение на русском языке с матом/грубостями/оскорблениями
- "target": переписанная версия

ПРАВИЛА ОБРАБОТКИ (важно!):

Сценарий 1 — ЕСЛИ в сообщении есть ОСКОРБЛЕНИЯ В АДРЕС СОБЕСЕДНИКА (ты, вы, обращение к человеку):
- Переписывай в аристократическом, изысканно-вежливом, культурном стиле
- Используй возвышенную лексику, изящные формулировки
- Полностью убирай грубость, агрессию, хамство
- Сохраняй суть претензии/мысли, но облекай в благородную форму
- Пример: "ты чё, совсем ёбнулся такие цены ставить?" → "Мне представляется, что установленная вами стоимость является чрезмерно завышенной и не соответствует истинной ценности данного продукта."

Сценарий 2 — ЕСЛИ оскорблений в адрес собеседника НЕТ (речь о ситуации, о третьих лицах, о себе):
- Просто заменяй матерные и грубые слова на литературные аналоги
- Сохраняй эмоциональный тон, разговорный стиль, структуру предложений
- НЕ переводи в аристократический/официальный стиль
- Пример: "блять вчера меня ебучий отец изнасиловал... опять" → "боже вчера мой отец надо мной надругался... вновь"

ТРЕБОВАНИЯ К INPUT:
- Живая разговорная русская речь
- Тематики: жалобы на сервис, конфликты с коллегами, семейные проблемы, личные переживания, рабочие ситуации, возмущение, отчаяние, раздражение
- Варьируй длину: от коротких фраз (2-4 слова) до развёрнутых сообщений (2-4 предложения)
- Варьируй степень агрессии: от лёгкого раздражения до открытого хамства
- Включай оба сценария: с оскорблениями собеседника и без

ПРИМЕРЫ:

Пример 1 (Сценарий 1 — оскорбление собеседника):
{
  "system": "Твоя задача — переписать сообщение пользователя в вежливой и культурной форме. Сохрани ТОЛЬКО смысл исходного сообщения. Не добавляй пояснений, обращений, подписей, приветствий. Работай кратко и точно. Если предложение не содержит оскорблений или токсичной лексики — оставь его без изменений. Ни в коем случае не меняй смысл! Никогда не меняй смысл. Выведи только — переписанный текст.",
  "input": "ты чё, совсем ёбнулся такие цены ставить? это же просто пицца, а не золотой слиток, ёпта!",
  "target": "Мне представляется, что установленная вами стоимость является чрезмерно завышенной и не соответствует истинной ценности данного продукта."
}

Пример 2 (Сценарий 2 — речь о третьем лице):
{
  "system": "Твоя задача — переписать сообщение пользователя в вежливой и культурной форме. Сохрани ТОЛЬКО смысл исходного сообщения. Не добавляй пояснений, обращений, подписей, приветствий. Работай кратко и точно. Если предложение не содержит оскорблений или токсичной лексики — оставь его без изменений. Ни в коем случае не меняй смысл! Никогда не меняй смысл. Выведи только — переписанный текст.",
  "input": "блять вчера меня ебучий отец изнасиловал... опять",
  "target": "боже вчера мой отец надо мной надругался... вновь"
}

Пример 3 (Сценарий 2 — речь о ситуации):
{
  "system": "Твоя задача — переписать сообщение пользователя в вежливой и культурной форме. Сохрани ТОЛЬКО смысл исходного сообщения. Не добавляй пояснений, обращений, подписей, приветствий. Работай кратко и точно. Если предложение не содержит оскорблений или токсичной лексики — оставь его без изменений. Ни в коем случае не меняй смысл! Никогда не меняй смысл. Выведи только — переписанный текст.",
  "input": "да сколько можно повторять одно и то же, блядь! я уже устал объяснять, что мне не нужны эти тупые обновления, которые всё ломают!",
  "target": "да сколько можно повторять одно и то же, чёрт! я уже устал объяснять, что мне не нужны эти дурацкие обновления, которые всё ломают!"
}

Выведи результат в виде валидного JSON-массива без каких-либо пояснений до или после."""


def check_ollama_running():
    """Проверяет, запущена ли Ollama."""
    try:
        response = requests.get("http://localhost:11434/api/tags", timeout=5)
        return response.status_code == 200
    except requests.exceptions.RequestException:
        return False


def get_available_models():
    """Получает список доступных моделей."""
    try:
        response = requests.get("http://localhost:11434/api/tags", timeout=5)
        if response.status_code == 200:
            models = response.json().get("models", [])
            return [model["name"] for model in models]
    except:
        pass
    return []


def extract_json(text):
    """Извлекает JSON-массив из ответа, обрезая незавершённый ответ."""
    # Убираем markdown-обёртки
    text = re.sub(r'```json\s*', '', text)
    text = re.sub(r'```\s*', '', text)
    text = text.strip()

    # Находим начало массива
    start = text.find('[')
    if start == -1:
        start = text.find('{')
        if start == -1:
            return None
        text = '[' + text[start:]
    else:
        text = text[start:]

    # Ищем закрывающую скобку массива
    end = text.rfind(']')
    if end != -1:
        candidate = text[:end + 1]
        try:
            json.loads(candidate)
            return candidate
        except json.JSONDecodeError:
            pass

    # Если массив не закрыт, собираем валидные объекты
    objects = re.findall(r'\{[^{}]*\}', text)
    if objects:
        fixed = '[' + ','.join(objects) + ']'
        try:
            json.loads(fixed)
            return fixed
        except json.JSONDecodeError:
            pass
    return None


def call_ollama(prompt, model):
    """Отправляет один запрос к Ollama и возвращает сгенерированный текст."""
    payload = {
        "model": model,
        "prompt": prompt,
        "stream": False,
        "options": {
            "temperature": 0.7,
            "top_p": 0.9
        }
    }
    response = requests.post(OLLAMA_URL, json=payload, timeout=600)
    if response.status_code != 200:
        # Возвращаем ошибку, чтобы цикл мог остановиться
        raise RuntimeError(f"Ошибка {response.status_code}: {response.text}")
    data = response.json()
    return data.get("response", "")


def main():
    global MODEL_NAME

    if not check_ollama_running():
        print("Ollama не запущена. Запустите командой: ollama serve")
        sys.exit(1)

    available_models = get_available_models()
    if available_models:
        print(f"Доступные модели: {', '.join(available_models)}")
        if MODEL_NAME not in available_models:
            print(f"Модель '{MODEL_NAME}' не найдена!")
            print("Выберите модель из списка или введите название: ", end="")
            new_model = input().strip()
            if new_model:
                MODEL_NAME = new_model
            else:
                MODEL_NAME = available_models[0] if available_models else "llama3"
            print(f"Используем модель: {MODEL_NAME}")

    all_data = []  # общий список всех собранных элементов

    # Попытка загрузить уже накопленные данные (если файл существует)
    try:
        with open(OUTPUT_FILE, 'r', encoding='utf-8') as f:
            existing = json.load(f)
            if isinstance(existing, list):
                all_data = existing
                print(f"Загружено {len(all_data)} элементов из предыдущего запуска.")
    except (FileNotFoundError, json.JSONDecodeError):
        pass

    print("\nНачинаем циклическую генерацию...")
    iteration = 0
    while True:
        iteration += 1
        print(f"\n--- Запрос №{iteration} ---")
        try:
            raw_text = call_ollama(PROMPT, MODEL_NAME)
        except Exception as e:
            print(f"Ошибка при обращении к Ollama: {e}")
            print("Предположительно закончились токены или доступ. Останавливаемся.")
            break

        # Сохраняем сырой ответ для отладки (последний)
        with open("raw_response_last.txt", "w", encoding="utf-8") as f:
            f.write(raw_text)

        json_str = extract_json(raw_text)
        if json_str is None:
            print("Не удалось извлечь JSON из ответа, пропускаем итерацию.")
            continue

        try:
            dataset = json.loads(json_str)
        except json.JSONDecodeError as e:
            print(f"Ошибка парсинга JSON: {e}, пропускаем итерацию.")
            continue

        if not isinstance(dataset, list):
            print("Ответ не является массивом JSON, пропускаем итерацию.")
            continue

        # Фильтруем валидные элементы
        valid_items = []
        for item in dataset:
            if isinstance(item, dict) and "input" in item and "target" in item:
                if "system" not in item:
                    item["system"] = (
                        "Твоя задача — переписать сообщение пользователя "
                        "в вежливой и культурной форме. Сохрани ТОЛЬКО смысл "
                        "исходного сообщения. Не добавляй пояснений, обращений, "
                        "подписей, приветствий. Выведи только — переписанный текст."
                    )
                valid_items.append(item)

        if not valid_items:
            print("Нет валидных элементов в этом ответе, пропускаем.")
            continue

        # Добавляем к общему списку
        all_data.extend(valid_items)
        print(f"Получено {len(valid_items)} элементов. Всего собрано: {len(all_data)}")

        # Сохраняем накопленный датасет в файл
        with open(OUTPUT_FILE, 'w', encoding='utf-8') as f:
            json.dump(all_data, f, ensure_ascii=False, indent=2)
        print(f"Данные сохранены в {OUTPUT_FILE}")

        # Небольшая пауза между запросами (чтобы не перегружать сервер)
        time.sleep(2)

    print(f"\nРабота завершена. Итоговый датасет сохранён в {OUTPUT_FILE}")
    print(f"Всего элементов: {len(all_data)}")


if __name__ == "__main__":
    main()